import { GenerateCommentsMsgPipe } from './generate-comments-msg.pipe';

describe('GenerateCommentsMsgPipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateCommentsMsgPipe();
    expect(pipe).toBeTruthy();
  });
});
