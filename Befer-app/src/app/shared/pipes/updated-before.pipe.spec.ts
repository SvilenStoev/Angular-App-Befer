import { UpdatedBeforePipe } from './updated-before.pipe';

describe('UpdatedBeforePipe', () => {
  it('create an instance', () => {
    const pipe = new UpdatedBeforePipe();
    expect(pipe).toBeTruthy();
  });
});
