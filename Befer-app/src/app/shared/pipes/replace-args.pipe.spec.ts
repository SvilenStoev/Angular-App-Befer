import { ReplaceArgsPipe } from './replace-args.pipe';

describe('ReplaceArgsPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceArgsPipe();
    expect(pipe).toBeTruthy();
  });
});
